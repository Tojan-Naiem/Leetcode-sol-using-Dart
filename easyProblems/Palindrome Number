class Solution {
  bool isPalindrome(int x) {

      if(x.isNegative)return false;
      int num=x;
      int sum=0;
      while(num>0)
      { 
          sum=sum*10+num%10;
          num~/=10;

      }
      if(sum==x)return true;
      else return false;
    
  }
}
